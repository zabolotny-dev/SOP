schema:
  - ../hosting-contracts/graphql/schema.graphqls

exec:
  filename: cmd/server/graphql/generated.go
  package: graphql

model:
  filename: cmd/server/graphql/models.go
  package: graphql

models:
  Server:
    fields:
      plan:
        resolver: true

resolver:
  layout: follow-schema
  dir: cmd/server/graphql
  package: graphql
