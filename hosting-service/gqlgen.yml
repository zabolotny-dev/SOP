schema:
  - ../hosting-contracts/graphql/schema.graphqls

exec:
  filename: internal/graph/generated.go
  package: graph

model:
  filename: internal/graph/models.go
  package: graph

models:
  Server:
    fields:
      plan:
        resolver: true

resolver:
  layout: follow-schema
  dir: internal/graph
  package: graph
